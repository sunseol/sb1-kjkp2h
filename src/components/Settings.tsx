import React from 'react';

const Settings: React.FC = () => {
  return (
    <div className="min-h-screen bg-gray-100 p-8">
      <h1 className="text-3xl font-bold mb-8">설정</h1>
      {/* 사용자 설정 및 계정 관리 인터페이스를 여기에 구현 */}
    </div>
  );
};

export default Settings;